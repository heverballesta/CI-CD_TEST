name: Saludo usando variables
on:
  workflow_dispatch
env:
  DIA_DE_SEMANA: lunes
jobs:
  saludo-variables:
    runs-on: ubuntu-latest
    env:
      SALUDO: hola
    steps:
      - name: Saludar
        run: echo "$SALUDO, $NOMBRE. Hoy es $DIA_DE_SEMANA!"
        env: 
          NOMBRE: Hever